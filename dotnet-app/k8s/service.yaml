apiVersion: v1
kind: Service
metadata:
  name: dotnet-app
  namespace: dotnet-app
  labels:
    app: dotnet-app
spec:
  selector:
    app: dotnet-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP